# 2026-08-17 — qiskit 2.3 port, local validation run

Manual (non-sweep) run validating the `fvm_euler_1d_solver` **qiskit-2.3 port**
against the stored **Apr 2026 Frontier** matched case. One trial, base-FVM HHL,
ideal statevector, 150k shots, nelem=5, cfl=1e10, BDF1, 15 outer / 1 inner —
byte-identical case fingerprint to April `trial_0_b9da8a4e`.

- Run host: Mac laptop `MH-DT9TLJQR2V` (NOT Frontier).
- Run time (UTC): 2026-08-17T20:11 → 20:25 (~817.7 s solver elapsed).
- Solver: `fvm_euler_1d_solver` `main` @ `60d973c5` + uncommitted qiskit-2.3 port.
- Linear-solver lib: `Q8020-CFD/quantum_linear_solvers` @ `66eadf0`.
- Stack: qiskit 2.3.1 / aer 0.17.2 / ibm-runtime 0.46.1 (matches April exactly).

## Layout

`trial_0/` — raw solver output + harvested metadata. The large per-iteration
`.qpy` circuit files (~1.5 GB) are NOT copied here (April keeps none either;
circuit stats live in `qc_metadata_*.csv` and `q8020_artifacts_*.json`). The
full run incl. `.qpy` remains at:
`/Users/agallojr/proj/src/research-notes/bus/agent-tmp/fvm-euler-1d-qiskit-1x-vs-2x/local-run-2026/`

### Metadata provenance

- `q8020_{case,code,backend,artifacts,results,analysis}_0.json` — generated by
  the repo harvester
  (`aux/fvm_euler_1d_nozzle/fvm_euler_1d_nozzle/fvm_euler_1d_solver_harvester.py --outdir`),
  same tool/schema the April sweep used for solver-side fragments.
- `q8020_{params,experiment,exec_stats}_local.json`,
  `q8020_env_{before,after}_local.json`, `q8020_code_provenance_local.json` —
  hand-authored, supplying the provenance the sweeper would normally add
  (command, host, timestamps, resolved package versions, git SHAs). Suffixed
  `_local` (not an 8-hex experiment id) to mark this as a manual run.

## Caveats

- **Timing is laptop wall-clock**, not comparable to Frontier (April matched
  trial was 2354.9 s incl. postproc; solver-only ~1653 s). Compare circuit size
  and solution quality, not absolute seconds.
- Full three-way (Nov 1.x / Apr 2.x / this) analysis:
  `/Users/agallojr/proj/src/q8020/q8020-cfd-experiments/aux/qiskit_2.x/fvm-euler-1d-1x-vs-2x-vs-local-2026-08/`
